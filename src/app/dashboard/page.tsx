import Link from "next/link";
import { ArrowRight, Bell, Check, Plus } from "lucide-react";
import { ActivityList, ProjectCard } from "@/components/cards";
import { projects } from "@/lib/data";
import { Pill, SectionHeading } from "@/components/app-shell";

export default function DashboardPage() { return <><div className="page-title"><div className="eyebrow">Good morning, Maya</div><h1>Your space to<br /><span style={{ color: "var(--lime)" }}>keep moving.</span></h1><p>Three small steps today become a body of work tomorrow. Here is what is waiting for you.</p></div><div className="dashboard-grid"><div className="panel"><div className="profile-progress"><div className="progress-ring"><strong>78%</strong></div><div className="progress-copy"><h3>Complete your profile</h3><p>Add your portfolio and one collaboration goal so the right people can find you.</p><Link href="/profile" className="section-link">Finish profile <ArrowRight size={13} /></Link></div></div></div><div className="panel"><h3><Bell size={15} style={{ verticalAlign: "middle", color: "var(--lime)" }} /> Notifications</h3><div className="activity-list"><div className="activity"><div className="activity-icon">NW</div><div>Noah commented on Lumen OS<span>8 minutes ago</span></div></div><div className="activity"><div className="activity-icon">+</div><div>New team invite<span>Yesterday</span></div></div></div></div></div><section className="section"><SectionHeading eyebrow="In motion" title="Your projects" action={<Link href="/projects" className="section-link">View all <ArrowRight size={13} /></Link>} /><div className="project-grid">{projects.slice(0, 2).map(project => <ProjectCard key={project.name} project={project} />)}<Link href="/projects" className="project-card" style={{ justifyContent: "center", alignItems: "center", color: "var(--muted)" }}><Plus size={20} /><span style={{ marginTop: 10, fontSize: 11 }}>Start something new</span></Link></div></section><div className="dashboard-grid section"><div className="panel"><SectionHeading eyebrow="Your trail" title="Recent activity" /><ActivityList /></div><div className="panel"><SectionHeading eyebrow="Earned" title="Achievements" /><div className="achievement"><div className="achievement-badge"><Check size={14} /></div><div>First contribution<span>Posted your first project update</span></div><Pill tone="lime">New</Pill></div><div className="achievement"><div className="achievement-badge">✦</div><div>Connector<span>Joined 3 communities</span></div></div></div></div></>; }