import { CalendarDays } from "lucide-react";
import { events } from "@/lib/data";
import { EventCard } from "@/components/cards";

export default function EventsPage() { return <><div className="page-title"><div className="eyebrow">The community calendar</div><h1>Make time for<br /><span style={{ color: "var(--lime)" }}>the good stuff.</span></h1><p>Workshops, competitions, open studios, and the occasional excuse to stay up too late making something.</p></div><div className="section-heading"><div><div className="eyebrow">September — October 2026</div><h2>Upcoming</h2></div><button className="button button-ghost"><CalendarDays size={15} /> Add to calendar</button></div><div className="event-grid">{events.map(event => <EventCard key={event.title} event={event} />)}</div><section className="section"><div className="section-heading"><div><div className="eyebrow">Archive</div><h2>Past events</h2></div></div><div className="panel feature-list"><div className="feature-row"><div className="event-date"><strong>22</strong></div><div><p>Build in public: summer session</p><span>128 attendees · 14 demos</span></div><span className="pill">Replay available</span></div><div className="feature-row"><div className="event-date"><strong>08</strong></div><div><p>Dropout Arena beta</p><span>32 players · 8 teams</span></div><span className="pill">Results</span></div></div></section></>; }